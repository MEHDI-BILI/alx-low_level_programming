#include <stdio.h>

/**
 * main - alphabet printed in lowercase z to a
 * Return: 0
 */

#include <stdio.h>

int main(void)
{
	char i;

	for (i = 'z' ; i <= 'a' ; i--)
		putchar(i);
	putchar('\n');
	return (0);
}
